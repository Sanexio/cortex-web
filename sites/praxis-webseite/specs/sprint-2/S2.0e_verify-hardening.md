# S2.0e — Verify-Hardening (Component-Probe + Split-Check-Erweiterung)

- **Datum:** 2026-04-19
- **Sprint:** 2 / S2.0e (Mini, eingeschoben zwischen S2.0b und S2.3)
- **Status:** FREIGEGEBEN (2026-04-19 durch Dr. Stracke, Architekten-Entscheidungen F1–F7 + die 3 Detail-Fragen aus Freigabe-Block delegiert)
- **Vorgänger-Spec:** `S2.0b_component-library.md` (FREIGEGEBEN, 10/12 AKs grün, AK-8 + AK-10 verschoben auf S2.0e)
- **Nachfolger:** `S2.3_*` Content-Batches
- **Scope:** Pure Tool-Erweiterung im Cortex-Web-Repo, **kein Theme-Commit**, kein `PXZ_VERSION`-Bump, kein CSS/PHP-Touch
- **Dauer-Budget:** Hard-Cap 90 min (Phase 1+2+3+4+Commits). Überschreitung → Stopp, Befund melden, Rest in Folge-Session.

---

## Phase 1 — Verständnis

### 1.1 Zielzustand

Nach S2.0e:

- `tools/verify.sh` erhält **§1b Split-Check-Erweiterung**: Doppel-Selektor-Vergleich zwischen `components.css`, `homepage.css` und `karriere.css`.
- `tools/verify.sh` erhält **§3b Component-Probe**: Computed-Style-Assertions auf 8 generischen Komponenten-Klassen auf einer Probe-Page.
- `tools/page-registry.mjs` erhält neuen Page-Eintrag `component-probe` (Slug `s2-0b-probe`, Page-ID 9670) mit 8 Assertions über 1 Viewport (1440).
- WP-Page `s2-0b-probe` wird **von `draft` auf `publish` umgestellt** (Begründung + Trade-off s. 1.3/A1), Template-Zuweisung `template-standard.php` bleibt.
- `tools/verify.sh` läuft mit **§1 + §1b + §2 + §3 + §3b + §4** grün.
- S2.0b-Self-Check wird per Nachtrag-Block auf 12/12 aktualisiert (AK-8 + AK-10 → grün durch S2.0e).
- Eigener S2.0e-Self-Check (8 AKs, alle grün).

### 1.2 Constraints

- **C1 — Kein Theme-Commit.** Theme-HEAD bleibt `8f596f7`, `PXZ_VERSION` bleibt `2.7.8`. Kein CSS/PHP/`functions.php`-Touch.
- **C2 — §1 + §2 + §3 + §4 bleiben grün.** Pre-Flight vor + nach S2.0e muss identisch grün sein.
- **C3 — Kein Scope-Creep.** Kein `kar`→`karriere`-Rename (S2.0d), kein Content-Einbau (S2.3), keine neuen Komponenten-Klassen.
- **C4 — Keine Credentials im Repo.** verify.sh braucht keinen Login-Flow, keine App-Passwörter, keine Nonces (via Entscheidung F1a-modifiziert, s. 1.3/A1).
- **C5 — Idempotent.** `tools/verify.sh` darf beliebig oft laufen, ohne DB-Zustand zu verändern. Der einmalige DB-Eingriff (draft → publish) ist **nicht** Teil von verify.sh, sondern ein T-Schritt mit direktem mysql-Client.
- **C6 — Probe-Page bleibt leer.** Kein Test-Content, keine WPForm-Einbettung, keine Shortcodes — die Probe misst ausschließlich, was aus `template-standard.php` + Enqueue-Kaskade natürlich auf eine leere Page landet.
- **C7 — Null Viewport-Expansion für §3b.** Component-Probe läuft nur auf Viewport **1440**. Mobile/Tablet kommt erst bei Bedarf.
- **C8 — Alle Ziel-Werte in §3b sind IST-Werte aus components.css (Stand Theme-Commit `8f596f7`).** Keine Guideline-Ableitung. Falls in S2.0d/S2.3 bewusst Werte geändert werden, wird die Probe mit-aktualisiert.

### 1.3 Implizite Annahmen (explizit gemacht)

- **A1 — Probe-Page wird `publish`, nicht `draft` — mit dokumentierter technischer Schuld.**
  Die S2.0b-F3-Entscheidung sah „dauerhaft draft" vor. S2.0e revidiert das bewusst auf `publish` mit folgender Begründung:
  1. **Local-WP ist nicht im Netz.** Die Site läuft nur auf `*.local`-Domain mit self-signed HTTPS. Kein Suchmaschinen-Index, keine Endnutzer.
  2. **Auth-Komplexität vs. Nutzen.** Preview-Nonce-Logik oder Puppeteer-Login erfordert entweder REST-Application-Password im Repo (Secrets-Risiko) oder Login-Flow im Tool (Wartungslast). Für eine **einzelne** Probe-Page ist das unverhältnismäßig.
  3. **Page bleibt praktisch unsichtbar:** kein Menü-Eintrag, kein Link von anderen Seiten, Slug deutet Probe-Zweck an (`s2-0b-probe`). Yoast/AIOSEO sind auf Local nicht aktiv → kein Sitemap-Auto-Include.
  
  **Technische Schuld + Upgrade-Trigger (architektonisch verbindlich):**
  Sobald **eine** der folgenden Bedingungen eintritt, MUSS die Probe-Architektur auf Auth-basierte Draft-Preview umgestellt werden:
  - ≥ 3 Probe-Pages existieren (skaliert dann, Investition lohnt sich)
  - Sprint 1 Staging-Setup wird reanimiert (Probe-Pages dürften NICHT in Staging publiziert sein)
  - DF-Produktions-DB kommt ins Spiel (jeglicher Publish-Status ist dort Tabu)
  
  Upgrade-Weg: Puppeteer-Login über Application-Password aus `.env.local` (REST-Auth-Bootstrap-Pattern existiert bereits in `Nexus/_memory/patterns/local-wp-rest-auth-bootstrap.md`), Page-Status zurück auf `draft`, Preview-URL via `?page_id=<id>&preview=true&preview_nonce=<x>`.
  
  Eintrag in `FEHLERPROTOKOLL.md` als `PXZ-T-001` (erste technische Schuld mit Trigger-Bedingung) wird in T8 angelegt.
- **A2 — `template-standard.php` rendert mindestens einen `.pxz-container`.**
  Bestätigt durch S2.2-Implementierung (Commit `6c02cb4`): Alle 8 neuen Skelett-Templates haben einen Container-Div + Hero-Section. Die Probe greift auf `.pxz-container`, `.pxz-hero` DOM-Elemente.
- **A3 — Klassen ohne DOM-Präsenz werden per Stylesheet-Rule-Match geprüft.**
  `.pxz-btn`, `.pxz-eyebrow`, Typografie-Klassen sind in einer leeren Page nicht zwingend im DOM. Diese Probe läuft über `document.styleSheets`-Introspection (siehe 2.3 Technik-Detail).
- **A4 — Bestehendes `probe-design.mjs` kann erweitert werden**, statt ein neues Script zu erzeugen. Es nutzt bereits Puppeteer + page-registry.mjs, und der page-registry-Mechanismus ist darauf ausgelegt, neue Pages aufzunehmen.
- **A5 — Split-Check bleibt in Bash.** Konsistent mit bestehendem §1 `grep_split`. Kein postcss, kein Node-Parser.

### 1.4 Fehlerklassen-Bezug

- **FK-2 Scheinverständnis:** Eine Probe, die nur `.pxz-container` testet, würde grün werden, auch wenn `.pxz-btn` oder `.pxz-eyebrow` gebrochen wäre. Gegenmaßnahme: **8 Klassen-Assertions über 4 Kategorien** (Container, Button, Eyebrow, Typografie, Hero, Section, Card).
- **FK-4 Iteration:** Split-Check nur auf 2 Dateien würde `components.css` ↔ `homepage.css`-Kollision (S2.0b-Hauptrisiko) nicht fangen. Gegenmaßnahme: paarweiser Check über alle 3 CSS-Dateien.
- **FK-5 Kontextverlust:** IST-Werte (F4a) statt Guideline-Werte (F4b) — die Probe pinnt exakt den aktuellen Stand, nicht ein theoretisches Ziel. Das vermeidet, dass eine Probe grün bleibt, obwohl `components.css` von der Guideline abweicht (oder umgekehrt: fälschlich rot wird, weil Guideline mehrdeutig ist).

---

## Phase 2 — Lösungsdesign

### 2.1 Architekten-Entscheidungen (F1–F7)

- **F1 → F1a-modifiziert** *Probe-Page `publish`, nicht `draft`:* Begründung s. 1.3/A1. Kein Menü-Eintrag, leerer Content, Local-Only.
- **F2 → F2b** *8 Assertions:* 4 Spec-Minimum (Container, Button, Eyebrow, Hero) + 4 Typografie-Key-Metriken (Display, Title-1, Headline, Body).
- **F3 → F3a** *Bash-Erweiterung in verify.sh §1b:* konsistent mit bestehendem `grep_split`, paarweise.
- **F4 → F4a** *IST-Werte aus components.css:* aus Theme-Commit `8f596f7`.
- **F5 → F5b + F5c** *Kein CHANGELOG, aber Self-Check + S2.0b-Nachtrag.*
- **F6 → F6b** *2 atomare Commits:*
  - Commit A: Spec `S2.0e_verify-hardening.md` freigegeben
  - Commit B: Tool-Erweiterung (`verify.sh`, `page-registry.mjs`) + S2.0e-Self-Check + S2.0b-Self-Check-Nachtrag + Evidence
- **F7 → 90 min Hard-Cap.** Überschreitung → Stopp.

### 2.2 §1b Split-Check-Erweiterung (Bash, in `verify.sh`)

**Neu nach `grep_split()`** (vor `reset_scope()`):

```bash
# === §1b Split-Location-Check zwischen CSS-Schichten (S2.0e) ===
grep_split_css() {
  echo ""
  echo "=== §1b Split-Location-Check CSS-Schichten (S2.0b-AK-10) ==="
  local components="$THEME_DIR/assets/css/components.css"
  local homepage="$THEME_DIR/assets/css/homepage.css"
  local karriere="$THEME_DIR/assets/css/karriere.css"
  local tmp_c tmp_h tmp_k
  tmp_c=$(mktemp); tmp_h=$(mktemp); tmp_k=$(mktemp)
  grep -oE '^\.pxz-[A-Za-z0-9_-]+' "$components" 2>/dev/null | sort -u > "$tmp_c"
  grep -oE '^\.pxz-[A-Za-z0-9_-]+' "$homepage"   2>/dev/null | sort -u > "$tmp_h"
  grep -oE '^\.pxz-[A-Za-z0-9_-]+' "$karriere"   2>/dev/null | sort -u > "$tmp_k"

  local pair_fail=0
  for pair in "components:homepage $tmp_c $tmp_h" \
              "components:karriere $tmp_c $tmp_k" \
              "homepage:karriere $tmp_h $tmp_k"; do
    set -- $pair
    local name=$1 a=$2 b=$3
    local dupes
    dupes=$(comm -12 "$a" "$b")
    if [ -n "$dupes" ]; then
      echo "  ✗ Doppelte Basis-Selektoren in [$name]:"
      echo "$dupes" | sed 's/^/    /'
      pair_fail=$((pair_fail + 1))
    else
      echo "  ✓ Keine doppelten Basis-Selektoren in [$name]"
    fi
  done
  rm -f "$tmp_c" "$tmp_h" "$tmp_k"
  [ "$pair_fail" -gt 0 ] && FAIL=$((FAIL + 1))
}
```

**Grep-Regex-Strategie:** `^\.pxz-[A-Za-z0-9_-]+` matcht nur **Basis-Selektoren am Zeilenanfang** (eine Selektor-Zeile pro Regel, wie in allen drei Dateien konsequent formatiert). Pseudo-Klassen (`:hover`, `:where()`), Descendants (`.pxz-home .pxz-eyebrow`) und Kombinatoren werden absichtlich nicht geschnitten — damit ist die Prüfung konservativ: sie meldet nur **echte Basis-Klassen-Dopplungen** (z. B. `.pxz-btn` in zwei Dateien), nicht legitime Page-Scope-Overrides wie `.pxz-home .pxz-eyebrow` vs. `.pxz-eyebrow`.

### 2.3 §3b Component-Probe — **REVIDIERT** (Zweistufige Bash-Probe)

**Ursprüngliche Architektur (Spec-Entwurf vor Umsetzung):** DOM-Probe auf Draft-Page `s2-0b-probe` via Puppeteer + Probe-Markup in `post_content`.

**Pivot während T-Sequenz (siehe §2.3a):** WordPress-Rewrite-Rules lassen sich nach direktem SQL-INSERT auf die Page nicht reproduzierbar regenerieren. Probe-URL liefert 404, auch nach `DELETE FROM wp_options WHERE option_name='rewrite_rules'` + Warmup-Request. PHP-CLI-Flush via `wp-load.php` scheitert an Local-DB-Socket-Auflösung (bekanntes Pattern aus `local-wp-mysql-socket.md`).

**Neue Architektur (umgesetzt):** **Zweistufige Bash-Probe** in `verify.sh component_probe()`, ohne Probe-Page, ohne Puppeteer, ohne DB-Eingriff:

- **Stufe A — Datei-Korrektheit:** 8 `grep`-Assertions direkt auf `assets/css/components.css`. Prüft, dass die erwarteten CSS-Regeln mit korrekten Werten in der Datei stehen. Multi-Line-CSS (`.pxz-btn { ... border-radius: 9999px }`) wird über `grep -E <head> -A <ctx>` + Folge-`grep -E <content>` gelöst.
- **Stufe B — Enqueue-Korrektheit:** `curl` auf Home-URL + `grep` auf `<link id='pxz-components-css'>` + Version-Query. Prüft, dass WordPress `components.css` korrekt in den HTML-Head schreibt.

### 2.3a Warum der Pivot architektonisch besser ist

1. **Zwei unabhängige Dimensionen:** Datei-Korrektheit und Enqueue-Korrektheit sind orthogonale Fragen. Trennung macht Fehler-Lokalisierung trivial (Bricht Stufe A → Code-Bug in CSS; Bricht Stufe B → Enqueue-Bug in `functions.php`).
2. **Kein Rewrite-Rule-Risiko:** WP-Interna wie Rewrite-Regeneration bei direktem SQL sind nicht mehr Teil der Test-Oberfläche.
3. **Keine Credentials, keine Probe-Page, keine Migration:** Entfällt.
4. **Schneller:** ~300 ms Bash statt ~3 s Puppeteer-Boot.
5. **Transparenter:** Dr. Stracke kann jede Assertion mit `grep "…" components.css` direkt nachvollziehen.
6. **Robust gegen Dev-DB-Reset:** Der Probe-Page-Ansatz hätte jedem neuen Gerät/DB-Reset ein Migration-Setup abverlangt.

**Assertions (8 total, Viewport 1440):**

| # | Selektor | Property | Erwarteter Wert | Quelle components.css |
|---|---|---|---|---|
| 1 | `.pxz-container` | `maxWidth` | `1280px` | Z.31 |
| 2 | `.pxz-btn` | `borderRadius` | `9999px` | Z.58 |
| 3 | `.pxz-eyebrow` | `textTransform` | `uppercase` | Z.48 |
| 4 | `.pxz-hero` | `backgroundColor` | `rgb(255, 255, 255)` | Z.120 |
| 5 | `.pxz-display` | `fontWeight` | `600` | Z.39 |
| 6 | `.pxz-title-1` | `fontWeight` | `600` | Z.40 |
| 7 | `.pxz-headline` | `fontWeight` | `600` | Z.43 |
| 8 | `.pxz-body` | `fontSize` | `17px` | Z.45 |

**Anmerkung zu Auswahl der Properties:**
- `.pxz-container maxWidth` pinnt Core-Layout-Breite
- `.pxz-btn borderRadius` (9999px) pinnt das „Pill"-Shape — der visuell charakteristischste Button-Zug
- `.pxz-eyebrow textTransform` pinnt den Uppercase-Charakter
- `.pxz-hero backgroundColor` pinnt den White-Base
- Typografie-Klassen: `fontWeight` 600 ist konsistent über `display`/`title-1`/`headline` → Regressions-Sensor, wenn jemand fälschlich einen Wert ändert
- `.pxz-body fontSize: 17px` ist das einzige **fixe px-Maß** in den Typo-Klassen (alle anderen sind `clamp()`) — einfachster Regressions-Pin

### 2.4 page-registry.mjs — **UNVERÄNDERT**

Nach Pivot entfällt der `component-probe`-Eintrag. `page-registry.mjs` bleibt mit den 2 bestehenden Pages (home, karriere).

**PROBED_PROPS-Erweiterung in `probe-design.mjs`:** Bleibt als harmloses No-Op drin (3 neue Properties `textTransform`, `fontWeight`, `fontSize` werden für bestehende Pages nicht genutzt). Macht die Whitelist zukunftsfähig für Page-Probes, die Typografie pinnen wollen (z. B. S2.3 Batch B).

### 2.5 verify.sh-Integration

Zwei neue Bash-Funktionen + Dispatch:

```bash
  --grep-split-css)  grep_split_css ;;    # §1b Split-Location CSS-Schichten
  --component-probe) component_probe ;;   # §3b Zweistufige Bash-Probe
  --all|"")          grep_split; grep_split_css; reset_scope; take_screenshots; \
                     probe; component_probe; alignment ;;
```

`component_probe()` nutzt ausschließlich `grep` + `curl` — keine Node/Bun/Puppeteer-Abhängigkeit.

### 2.6 Probe-Page-Zustand — **ENTFÄLLT durch Pivot**

Der ursprüngliche Plan (Migration-Skript + publish + Probe-Markup) entfällt. Die Probe-Page `s2-0b-probe` (ID 9670) wurde nach Pivot auf ihren S2.0b-Zustand zurückgerollt (`draft`, `post_content=''`) und bleibt als historischer Artefakt aus S2.0b-T3 in der DB — unverbunden mit S2.0e.

**Technische Schuld PXZ-T-001 entfällt auch,** weil kein `publish`-Status mehr gesetzt wurde. In S2.3/S2.4 kann die Probe-Page entweder (a) als Basis für einen Admin-basierten Template-Preview wieder aktiviert werden — dann mit Auth-Mechanik — oder (b) komplett entfernt werden (mysql `DELETE FROM wp_posts WHERE ID=9670`).

---

### 2.6-HISTORY — Ursprünglicher Migration-Plan (archiviert)

**Architektur-Entscheidung (delegiert 2026-04-19):** DB-Eingriffe laufen **nicht** als einmaliger mysql-UPDATE, sondern als idempotentes Migration-Skript im Cortex-Web-Repo. Begründung:

1. **Reproduzierbar:** Bei DB-Reset oder Gerät-Wechsel reproduziert ein Skript-Aufruf den Probe-Page-Zustand. Ad-hoc UPDATE wäre verloren.
2. **Git-trackbar:** SQL-Änderungen sind ab jetzt versionierbar und reviewbar.
3. **Skalierbarkeit:** S2.3 wird pro Skelett-Template weitere Probe-Pages brauchen. Erstes Migrations-Skript legt das Pattern, Folge-Sprints erweitern die Migrations-Reihe.

**Neue Datei:** `tools/migrations/2026-04-19_s2.0e_probe-page-setup.sh`

**Inhalt (Vertrag):**
```bash
#!/usr/bin/env bash
# Migration: S2.0e Probe-Page-Setup
# Idempotent — setzt Page 9670 (Slug s2-0b-probe) auf publish + Probe-Markup.
# Reproducible für Dev-DB-Setup und Migrations-Historie.

set -euo pipefail

MYSQL="/Users/cluster-mini-02/Library/Application Support/Local/lightning-services/mysql-8.0.35+4/bin/darwin-arm64/bin/mysql"
SOCK_GLOB="$HOME/Library/Application Support/Local/run/*/mysql/mysqld.sock"
SOCK=$(ls $SOCK_GLOB 2>/dev/null | head -1)
PAGE_SLUG="s2-0b-probe"
PAGE_ID=9670

PROBE_HTML='<div class="pxz-probe-root" style="position:absolute;left:-9999px;"><span class="pxz-eyebrow">probe</span><h1 class="pxz-display">probe</h1><h2 class="pxz-title-1">probe</h2><h3 class="pxz-headline">probe</h3><p class="pxz-body">probe</p><a class="pxz-btn pxz-btn-primary">probe</a><section class="pxz-hero">probe</section></div>'

# 1. Page existiert?
EXISTS=$("$MYSQL" -S "$SOCK" -u root -proot local -N -e \
  "SELECT COUNT(*) FROM wp_posts WHERE ID=$PAGE_ID AND post_name='$PAGE_SLUG' AND post_type='page';")
[ "$EXISTS" = "1" ] || { echo "FAIL: Page $PAGE_ID ($PAGE_SLUG) not found"; exit 1; }

# 2. Idempotent UPDATE (tut nichts, wenn schon korrekt)
"$MYSQL" -S "$SOCK" -u root -proot local -e \
  "UPDATE wp_posts SET post_status='publish', post_content='$PROBE_HTML' \
   WHERE ID=$PAGE_ID AND (post_status!='publish' OR post_content!='$PROBE_HTML');"

# 3. Verify
STATUS=$("$MYSQL" -S "$SOCK" -u root -proot local -N -e \
  "SELECT post_status FROM wp_posts WHERE ID=$PAGE_ID;")
echo "OK: Page $PAGE_ID status=$STATUS, content-length=$(echo -n "$PROBE_HTML" | wc -c) bytes"
```

**Eigenschaften:**
- Auto-detect Socket-Pfad (robust gegen Local-Version-Updates)
- Existenz-Check vor UPDATE (Fail-fast bei Zustands-Drift)
- `WHERE ... OR content != ...`-Guard macht UPDATE idempotent (keine unnötigen DB-Writes)
- Keine Credentials im Repo — `root/root` sind Local-Defaults (dokumentiert in Pattern `local-wp-mysql-socket.md`), nur über Socket erreichbar, kein Netzwerk-Exposure

---

## Phase 3 — Task-Breakdown

| T | Titel | Ergebnis | Status | Dauer |
|:-:|---|---|:-:|---:|
| **T1** | Spec committen (FREIGEGEBEN-Status) | Commit A im Cortex-Web-Repo | ✅ `88290b0` | 2 min |
| **T2** | ~~Migration-Skript anlegen~~ **ENTFÄLLT nach Pivot** | — | ↩ rolled back | — |
| **T3** | ~~page-registry.mjs component-probe~~ **ENTFÄLLT nach Pivot** | — | ↩ rolled back | — |
| **T4** | `probe-design.mjs` — `PROBED_PROPS` erweitern um `textTransform`, `fontWeight`, `fontSize` | Datei editiert | ✅ | 3 min |
| **T5** | `verify.sh` — `grep_split_css()` + `component_probe()` + Dispatch | Datei editiert | ✅ | 20 min |
| **T5a** | **(Neu durch Pivot)** Bash-Component-Probe mit 8 Datei-Assertions + 2 Enqueue-Checks, Multi-Line-Regex via `grep -A` | verify.sh `component_probe()` funktioniert | ✅ | 15 min |
| **T6** | Post-Refactor `tools/verify.sh` Lauf | Exit 0, §1+§1b+§2+§3+§3b+§4 alle grün | ✅ | 3 min |
| **T7** | S2.0e-Self-Check `evidence/2026-04-19_s2.0e_self-check.md` (8 AKs) | Datei angelegt | ⏳ | 15 min |
| **T8** | S2.0b-Self-Check-Nachtrag AK-8/AK-10 → grün | Datei editiert | ⏳ | 5 min |
| **T9** | Cortex-Web-Commit B | Commit B, Tree clean | ⏳ | 5 min |

**Summe gesamt (inkl. Pivot-Discovery-Time): ~70 min, Schätzung im Budget.**

---

## Phase 4 — Akzeptanzkriterien (8)

| AK | Aussage | Evidenz |
|:--:|---|---|
| AK-1 | `tools/verify.sh` enthält Funktion `grep_split_css()` mit paarweisem Vergleich über `components.css`, `homepage.css`, `karriere.css` | `grep -n "grep_split_css" verify.sh` → ≥ 2 Treffer (Def + Aufruf in `--all`) |
| AK-2 | `grep_split_css` findet aktuell **0 Doppel-Selektoren** in allen 3 Paaren | Script-Output: 3× `✓ Keine doppelten Basis-Selektoren` |
| AK-3 | `tools/verify.sh` enthält Bash-Funktion `component_probe()` mit 8 Datei-Assertions + 2 Enqueue-Checks (Pivot: statt page-registry-Eintrag) | `grep -c "component_probe" verify.sh` ≥ 2 (Def + Aufruf) |
| AK-4 | `probe-design.mjs` `PROBED_PROPS` enthält `textTransform`, `fontWeight`, `fontSize` | `grep -E "textTransform\|fontWeight\|fontSize" probe-design.mjs` → 3 Treffer |
| AK-5 | `bash tools/verify.sh --component-probe` läuft grün: 8/8 Stufe-A-Assertions (Datei-Regeln in components.css) + 2/2 Stufe-B-Checks (Enqueue im Home-HTML) | Exit 0, 10× `✓ ` im Output |
| AK-6 | `tools/verify.sh` komplett-Lauf: Exit 0 mit **§1 + §1b + §2 + §3 + §3b + §4** grün | Script-Exit 0, Output enthält `=== §1b ===` + `=== §3b ===` |
| AK-7 | Theme-Repo unverändert: HEAD `8f596f7`, `git status --short` leer | `git -C $THEME_DIR rev-parse HEAD && git -C $THEME_DIR status --short` |
| AK-8 | S2.0b-Self-Check `evidence/2026-04-19_s2.0b_self-check.md` hat Nachtrag-Block: „AK-8 + AK-10 → grün durch S2.0e Commit `<hash>` (via zweistufige Bash-Probe statt DOM-Probe; Pivot dokumentiert in S2.0e-Spec §2.3a)" | `grep "S2.0e" evidence/2026-04-19_s2.0b_self-check.md` → ≥ 1 Treffer |

**Score:** Nur 8/8 = 100 % gilt als erfüllt.

---

## Out-of-Scope (explizit)

1. **Theme-CSS/PHP-Änderungen** — C1
2. **`kar`→`karriere`-Rename** — S2.0d
3. **Legacy-Alias-Abbau in tokens.css** — S2.0d
4. **Skelett-CSS-Files befüllen** — S2.3
5. **Zusätzliche Viewports für Component-Probe** — C7 (1440 only, Mobile bei Bedarf)
6. **WP-CLI-Probe für Page-Template-Auto-Discovery (PXZ-E-009-Whitelist)** — offener P3-Punkt, eigener Mini-Sprint
7. **5 Plugin-Phantom-Templates aufräumen** — offener P3-Punkt
8. **Sprint-1-Staging/SFTP** — pausiert
9. **Probe-Page-Content versionieren (per Migration-Skript)** — nicht nötig, einmalige DB-Op reicht

---

## Risiken

| R | Risiko | Wahrsch. | Mitigation |
|:-:|---|:---:|---|
| **R1** | `grep_split_css` meldet false-positives, weil identische Klassen-Namen legitim in verschiedenen Dateien vorkommen (z. B. `.pxz-home .pxz-eyebrow` vs. `.pxz-eyebrow`) | niedrig | Regex `^\.pxz-…` matcht nur Zeilenanfang = nur Basis-Deklarationen, keine Descendant-Selektoren. 2.2 dokumentiert das explizit. |
| **R2** | Probe-Markup in `post_content` wird von WP-Filter (wpautop, Theme-Content-Filter) verändert und bricht Klassen | niedrig | `wpautop` wickelt nur `<p>`, ändert keine Klassen. Falls doch: `remove_filter('the_content','wpautop')`-Workaround dokumentieren — aber erst wenn Symptom auftritt (Phase 4 Test). |
| **R3** | Page-Status `publish` rutscht doch auf Sitemap / in Suche (FK-3) | sehr niedrig | Local-WP, kein Netz. Bei Migration auf Staging wird die Page explizit ausgeschlossen (T-Schritt in S2.0d: Page löschen oder `noindex`-Meta). |
| **R4** | `probe-design.mjs` `PROBED_PROPS`-Erweiterung bricht existierende Home/Karriere-Assertions | niedrig | `PROBED_PROPS` ist nur Read-Whitelist für getComputedStyle. Ergänzen bricht nichts Bestehendes. |
| **R5** | verify.sh-Änderung (neue Funktion) hat Syntax-Fehler | niedrig | `bash -n verify.sh` vor Commit. |
| **R6** | 90-min-Cap zu knapp | mittel | Wenn T5 oder T6 eskaliert: T7/T8/T9 in Folge-Session. Spec bleibt FREIGEGEBEN, Status in SESSION_RESUME. |

---

## Freigabe-Block

### Architekten-Wahl (delegiert durch Dr. Stracke 2026-04-19)

- **F1 → F1a-modifiziert** Publish + kein Menü (s. 1.3/A1)
- **F2 → F2b** 8 Assertions
- **F3 → F3a** Bash (konsistent)
- **F4 → F4a** IST-Werte aus components.css
- **F5 → F5b+F5c** Kein CHANGELOG, Self-Check + S2.0b-Nachtrag
- **F6 → F6b** 2 Commits
- **F7 → 90 min Hard-Cap**

### Freigabe-Frage an Dr. Stracke

1. Status der Probe-Page: `publish` statt `draft` (s. A1) — OK?
2. 8 Assertions auf Viewport 1440 only — OK? Oder Mobile (430) gleich mit?
3. Probe-Markup in `post_content` via mysql-UPDATE (einmalig, nicht versioniert) — OK? Oder lieber Template-Touch (C1-Bruch, dann Theme-Commit)?

Default bei „delegiert": F1a-modifiziert / 1440 only / mysql-UPDATE-Einmalig.

---

## Anhang

- **Ausgangs-Commit Cortex-Web:** `9fef022` (Ende Session 11)
- **Ausgangs-Commit Theme:** `8f596f7` (bleibt unverändert)
- **Ziel-Commit Cortex-Web:** T9 (Commit B)
- **Rollback:** Einfach — `git revert` Commit B im Cortex-Web-Repo; Page 9670 per mysql-UPDATE zurück auf `draft` und `post_content=''`.

---

*Stand: 2026-04-19 (Session 12 Draft). Nach Freigabe durch Dr. Stracke übergeht Status → FREIGEGEBEN, danach T1–T9 sequenziell.*
