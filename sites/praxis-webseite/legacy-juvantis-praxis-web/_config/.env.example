# praxis-web — WordPress-Zugang
# Diese Datei ist ein Template. Kopiere sie zu `.env` und trage die echten Werte ein.
# `.env` selbst ist via .gitignore ausgeschlossen und darf NICHT ins Repo.

WP_URL="https://westend-hausarzt.com"
WP_USER=""
WP_APP_PASSWORD=""

# Staging (wird in Phase 4 eingetragen)
WP_STAGING_URL="http://praxis-stracke-staging.local"
WP_STAGING_USER=""
WP_STAGING_APP_PASSWORD=""
