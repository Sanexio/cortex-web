{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cortex-web.local/schemas/product-v1.json",
  "title": "Product",
  "description": "Cortex-Web Produkt (Bluttest, Body-Check etc.). I-2 hybrid i18n: sprach-invariante Felder auf Top-Level, mehrsprachige Felder verschachtelt unter .de/.en/.fr/.es. HWG-Gate: views.praxis.show_price MUSS false sein.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "sku", "category", "status", "parameters", "title", "views"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Stabile Kennung, wird als WP-Page-Slug verwendet"
    },
    "sku": {
      "type": "string",
      "description": "Shopify-SKU (sprach-invariant)"
    },
    "category": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "status": {
      "enum": ["active", "draft", "archived"]
    },
    "price_eur": {
      "type": "number",
      "minimum": 0,
      "description": "Juvantis-Preis, wird von Praxis-View ignoriert (HWG)"
    },
    "images": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "hero": { "type": "string", "pattern": "^media://" },
        "gallery": {
          "type": "array",
          "items": { "type": "string", "pattern": "^media://" }
        }
      }
    },
    "parameters": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["code", "einheit"],
        "properties": {
          "code": { "type": "string", "minLength": 1 },
          "einheit": { "type": "string", "minLength": 1 }
        }
      }
    },
    "title": { "$ref": "#/definitions/i18nString" },
    "tagline": { "$ref": "#/definitions/i18nString" },
    "beschreibung": { "$ref": "#/definitions/i18nString" },
    "views": {
      "type": "object",
      "additionalProperties": false,
      "required": ["juvantis", "praxis"],
      "properties": {
        "juvantis": {
          "type": "object",
          "additionalProperties": false,
          "required": ["show_price", "cta_label", "cta_url"],
          "properties": {
            "show_price": { "type": "boolean" },
            "cta_label": { "$ref": "#/definitions/i18nString" },
            "cta_url": { "type": "string", "minLength": 1 }
          }
        },
        "praxis": {
          "type": "object",
          "additionalProperties": false,
          "required": ["show_price", "cta_label", "cta_url"],
          "properties": {
            "show_price": {
              "type": "boolean",
              "const": false,
              "description": "HWG: Praxis-View darf niemals Preis anzeigen"
            },
            "headline_override": { "$ref": "#/definitions/i18nString" },
            "cta_label": { "$ref": "#/definitions/i18nString" },
            "cta_url": {
              "type": "string",
              "pattern": "^(/|https://sanexio\\.eu/)",
              "description": "Praxis-CTA darf nur intern (/) oder auf die zugelassene externe Shop-Domain zeigen. HINWEIS: Die Domain im pattern ist heute auf den Reference-Tenant (sanexio.eu) hartcodiert — Parametrisierung via Tenant-Schema-Override ist offenes Architektur-Item (siehe Plattform-§10)."
            }
          }
        }
      }
    },
    "sanexio_source": {
      "type": "object",
      "description": "Provenance-Block (drift-sync seit 2026-04-29). Verknüpft Trunk-YAML mit Sanexio-Source. Auch bei lokal editiertem Praxis-Content bleibt der Bezug erhalten. Spec: specs/drift-sync/SPEC.md §E2.",
      "required": ["type", "handle", "resource_id", "last_synced_at", "last_synced_hash"],
      "additionalProperties": false,
      "properties": {
        "type": { "type": "string", "enum": ["page", "product"] },
        "handle": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "collection": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "resource_id": { "type": "integer", "minimum": 1 },
        "scope": { "type": "string", "enum": ["labor", "untersuchungen", "body-checks", "pages-shared", "ultraschall", "funktion", "check-up"] },
        "last_synced_at": { "type": "string", "format": "date-time" },
        "last_synced_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "drift_strategy": { "type": "string", "enum": ["auto-curate", "manual", "frozen"], "default": "auto-curate" },
        "local_edits": { "type": "boolean", "default": false }
      }
    }
  },
  "definitions": {
    "i18nString": {
      "type": "object",
      "required": ["de"],
      "additionalProperties": false,
      "properties": {
        "de": { "type": "string", "minLength": 1 },
        "en": { "type": "string", "minLength": 1 },
        "fr": { "type": "string", "minLength": 1 },
        "es": { "type": "string", "minLength": 1 }
      }
    }
  }
}
